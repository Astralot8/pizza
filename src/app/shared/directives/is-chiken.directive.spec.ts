import { IsChikenDirective } from './is-chiken.directive';

describe('IsChikenDirective', () => {
  it('should create an instance', () => {
    const directive = new IsChikenDirective();
    expect(directive).toBeTruthy();
  });
});
